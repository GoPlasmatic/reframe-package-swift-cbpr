{
    "description": "Workflow loading order for Message Validation",
    "workflows": [
        {"path": "00-detect-format.json", "description": "Detects message format (MT or MX) and sets direction"},
        {"path": "validate-mt.json", "description": "Validate SWIFT MT messages"},
        {"path": "validate-mx.json", "description": "Validate ISO 20022 MX messages"}
    ]
}
