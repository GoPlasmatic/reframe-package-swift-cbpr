{
    "id": "validate-mt",
    "name": "Validate MT Message",
    "description": "Validates SWIFT MT messages using the validate_mt function",
    "priority": 1,
    "condition": {"==": [{"var": "metadata.message_type"}, "MT"]},
    "tasks": [
        {
            "id": "validate_mt_message",
            "name": "Validate MT Message",
            "description": "Uses the validate_mt function to validate the MT message",
            "function": {"name": "validate_mt", "input": {"source": "payload", "target": "output"}}
        }
    ]
}
